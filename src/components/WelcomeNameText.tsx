import Typography from "@mui/material/Typography";
import * as React from "react";

export default function WelcomeName() {
    return (
        <Typography variant="h2">Hej Stefan!</Typography>
        );
}